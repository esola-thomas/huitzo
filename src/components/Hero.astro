---
import TerminalPrompt from './TerminalPrompt.astro';
import CTAButton from './CTAButton.astro';

export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

const terminalLines = [
  '!$ huitzo --about',
  'i> Intelligence-as-a-Service Platform',
  's> Democratize intelligent data analysis',
  'w> Build powerful tools with simple CLI commands',
  'e> _'
];
---

<section class={`py-20 lg:py-32 ${className}`}>
  <div class="container mx-auto px-4">
    <div class="max-w-6xl mx-auto">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <!-- Content -->
        <div>
          <h1 class="text-4xl lg:text-6xl font-bold text-text-primary mb-6 leading-tight">
            Intelligence-as-a-Service
            <span class="text-gradient animated-gradient block">Platform</span>
          </h1>

          <p class="text-xl text-text-secondary mb-8 leading-relaxed">
            Democratize intelligent data analysis through a plugin-based architecture.
            Configure sophisticated analysis with simple terminal commands that anyone can learn.
          </p>

          <div class="flex flex-col sm:flex-row gap-4 mb-12">
            <CTAButton href="https://cli.huitzo.com" target="_blank" rel="noopener">
              Launch CLI (COMING SOON) →
            </CTAButton>
            <CTAButton href={`${import.meta.env.BASE_URL}plugins`} variant="secondary">
              Explore Plugins
            </CTAButton>
          </div>

          <!-- Key Stats -->
          <div class="grid grid-cols-3 gap-6 pt-8 border-t border-huitzo-border">
            <div>
              <div class="text-2xl font-bold text-huitzo-accent font-mono">7</div>
              <div class="text-sm text-text-muted">Month Timeline</div>
            </div>
            <div>
              <div class="text-2xl font-bold text-huitzo-accent font-mono">1</div>
              <div class="text-sm text-text-muted">Launch Plugin</div>
            </div>
            <div>
              <div class="text-2xl font-bold text-huitzo-accent font-mono">May '26</div>
              <div class="text-sm text-text-muted">Public Launch</div>
            </div>
          </div>
        </div>

        <!-- Terminal Demo -->
        <div class="lg:pl-8">
          <TerminalPrompt lines={terminalLines} animate={true} class="glow" />

          <!-- Features Preview -->
          <div class="mt-8 grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div class="bg-huitzo-secondary/50 border border-huitzo-border rounded-lg p-4">
              <div class="text-lg mb-2">🔌</div>
              <div class="text-sm font-medium text-text-primary">Plugin-Based</div>
              <div class="text-xs text-text-muted">Extensible architecture</div>
            </div>
            <div class="bg-huitzo-secondary/50 border border-huitzo-border rounded-lg p-4">
              <div class="text-lg mb-2">💻</div>
              <div class="text-sm font-medium text-text-primary">CLI Interface</div>
              <div class="text-xs text-text-muted">Simple commands</div>
            </div>
            <div class="bg-huitzo-secondary/50 border border-huitzo-border rounded-lg p-4">
              <div class="text-lg mb-2">🚀</div>
              <div class="text-sm font-medium text-text-primary">Automated</div>
              <div class="text-xs text-text-muted">Intelligent insights</div>
            </div>
            <div class="bg-huitzo-secondary/50 border border-huitzo-border rounded-lg p-4">
              <div class="text-lg mb-2">📊</div>
              <div class="text-sm font-medium text-text-primary">Data Analysis</div>
              <div class="text-xs text-text-muted">Any domain</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>